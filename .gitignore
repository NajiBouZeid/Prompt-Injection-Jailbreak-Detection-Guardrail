# Python virtual environment
venv/
venv-llmguard/
__pycache__/
*.pyc

# Environment variables / secrets (API keys go here later - NEVER commit this file)
.env

# Datasets and model weights - regenerable, too large for git
/data/
/models/
*.bin
*.safetensors
*.pt
*.ckpt

# HuggingFace cache if it ends up local to the project
.cache/

# Jupyter checkpoints
.ipynb_checkpoints/

# Eval/comparison outputs, logs, and other regenerable run artifacts
eval_report*.json
leakage_report*.json
leakage_examples.txt
qualifire_language_report.json
threshold_sweep_*.json
train_loss_history.json
train_loss_sampled.json
*.log

# OS/editor clutter
.DS_Store
.vscode/
